## Steps to run the Code:

1. `Create an .env file and put the API keys for fetching the model and other required tools.`

    `e.g.,`
    - `GROQ_API_KEY="API KEY"`
    - `GOOGLE_API_KEY="API KEY"`

2. run `pip install -r requirements.txt -y`